<% const d = new Date(); let year = d.getFullYear(); %>
<footer>
  <p class="copyright">&copy;<%= year %>, CSE 340 App</p>
  <% if (typeof intError !== 'undefined') { %> <!-- ✅ Prevent error if intError is missing -->
    <%- intError %>
  <% } %>
</footer>
